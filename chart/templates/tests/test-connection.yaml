apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloud-server.fullname" . }}-test-connection"
  labels:
    {{- include "cloud-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloud-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
